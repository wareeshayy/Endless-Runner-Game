namespace ZillRunner.Obstacles
{
    public enum ObstacleType
    {
        /// <summary>Original barrier1 — avoid with jump OR slide.</summary>
        FullBarrier,
        /// <summary>Original barrier2 — avoid with jump only.</summary>
        JumpBarrier
    }
}
